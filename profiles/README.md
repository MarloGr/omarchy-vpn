# WireGuard profiles

Place provider-exported WireGuard client configurations here as `*.conf`.
The filename without `.conf` is the profile ID displayed by Omarchy VPN.

Profiles may contain their real `PrivateKey`, or a placeholder when a shared
key is supplied with `../pk` or `install.sh --pk FILE`.
